import socket
import time

if __name__ == '__main__':
    # client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    # client.settimeout(10)
    # client.connect(("127.0.0.1", 8001))
    # client.sendall(b'test.txt')
    # print(client.recv(8192))
    print(time.time())
